💡 Qué debes saber antes

En JSON (JavaScript Object Notation):

Los datos se estructuran como objetos { ... } y arreglos (arrays) [ ... ].

Cada objeto representa un “elemento”.

Los objetos dentro de un array se separan con comas.

Las claves van entre comillas dobles " ".